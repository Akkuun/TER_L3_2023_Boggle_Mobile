export function create_game(req: any, res: any) {
    res.send('Create game')

    //if player is not in a game
    //create a new game 
    //add player to game
    //add player to player_in_game_list

    //else 
    //send error message
}